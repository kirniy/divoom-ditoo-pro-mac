# Divoom D2 Pro Mac Morning Verify Plan

- [ ] Rebuild the menu bar app and ensure the current source tree produces a fresh app bundle
- [ ] Verify the scene-color path still succeeds on the Ditoo Pro `16x16` RGB display
- [ ] Verify the pixel-test path still succeeds on the Ditoo Pro `16x16` RGB display
- [ ] Verify the native animation-upload path still succeeds against the live menu bar app over IPC
- [ ] If any of the above fail, fix them before doing anything else
- [ ] Continue reverse-engineering and implementation work on actual on-device animation playback
